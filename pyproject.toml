[project]
name = "taskmaster"
version = "0.1.0"
dependencies = [
    "pydantic>=2.13.4",
    "pyyaml>=6.0.3",
]

[project.scripts]
server = "server:main"
client = "client:main"

[tool.uv]
package = true
